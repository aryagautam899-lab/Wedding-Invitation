const entry=document.getElementById("entry"),openBtn=document.getElementById("openInvitation"),music=document.getElementById("music"),musicBtn=document.getElementById("musicBtn"),musicText=document.getElementById("musicText"),menuBtn=document.getElementById("menuBtn"),nav=document.getElementById("nav");
openBtn.addEventListener("click",async()=>{entry.classList.add("hide");document.body.classList.remove("locked");try{await music.play();musicBtn.textContent="Ⅱ";musicText.textContent="Playing"}catch{musicText.textContent="Tap to play"}});
musicBtn.addEventListener("click",async()=>{if(music.paused){try{await music.play();musicBtn.textContent="Ⅱ";musicText.textContent="Playing"}catch{musicText.textContent="Tap again"}}else{music.pause();musicBtn.textContent="♪";musicText.textContent="Paused"}});
menuBtn.addEventListener("click",()=>nav.classList.toggle("open"));nav.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>nav.classList.remove("open")));
const target=new Date("2026-08-15T19:30:00+05:30").getTime();
function tick(){const d=target-Date.now();if(d<=0)return;const v={days:Math.floor(d/86400000),hours:Math.floor((d%86400000)/3600000),minutes:Math.floor((d%3600000)/60000),seconds:Math.floor((d%60000)/1000)};for(const[k,val]of Object.entries(v)){const e=document.getElementById(k);if(e)e.textContent=String(val).padStart(2,"0")}}tick();setInterval(tick,1000);